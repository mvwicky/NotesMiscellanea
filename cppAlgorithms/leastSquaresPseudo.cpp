#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

int main(){
	const static int n(34), m(14);
	int M[n][m] = {0};
	for (int k = 0; k < n; k++){
		M[k][1] = 1;
		for (int j = 1; j < m; j++){
			M[k][j] = 
		}
	}
}