#include <iostream>
#include <vector>
#include <ctime>
#include <cmath>
#include <cstdlib>

#include "vec3.h"
#include "boid.h"
#include "flock.h"

#define DEF_DIM 100

double randInRange(double min, double max){
	return min + double(rand()) / (double(RAND_MAX) / (max - min));
}

double randOutRange(double min, double max){

}




int main(){
	return 0;
}