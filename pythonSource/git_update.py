import os
import sys
import subprocess


def main():
    cwd = os.getcwd()
    if 'GitHub' not in cwd:
        print('Must be in a path with a GitHub folder')
        sys.exit(-1)
    git = os.


if __name__ == '__main__':
    main()
